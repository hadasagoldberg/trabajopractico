import {createTheme} from '@mui/material/styles';

const theme = createTheme({
    palette: {
        secondary: {
            main: '#ffffff',
            light: '#ede7f6',
            dark: '#bbb5c3'

        }
    }
})

export default theme;